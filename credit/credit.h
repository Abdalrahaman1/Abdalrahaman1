#ifndef CREDIT_H_INCLUDED
#define CREDIT_H_INCLUDED

#include <stdio.h>
#include <stdlib.h>


typedef enum
{
    false = 0, true
}boolean;


void credit (void);
int size (char*x);
boolean check (char* p2s);


#endif // CREDIT_H_INCLUDED
